import styled  from '@emotion/styled';

export const RedInput = styled.input `
  border: 1px solid red;


`
export const Button = styled.button `
  background-color: ${(props)=> props.qqq ? "yellow" : 'green'}
  
 

`